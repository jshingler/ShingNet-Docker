# Setup Info

Mirror this at `/var/www/html`

Key Commands to make this happen
```
cd /var/www/html/
sudo mkdir portal
sudo mount --bind /home/jsingler/docker/www/html/portal /var/www/html/portal
```

Maintain as part of ShingNet-Docker

Explains the idea:
https://github.com/jshingler/ShingNet/issues/3
